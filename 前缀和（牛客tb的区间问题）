#include <bits/stdc++.h>
#include <type_traits>
using namespace std;
#define int long long
struct node
{
    int num;
    int f;
};
// int n;
// vector<int>a(n+1);
// vector<int>sum(n+1);
signed main() {
    int n,k;
    cin>>n>>k;
//     vector<node>a(n+1);
//     for(int i=1;i<=n ;i++)
//    {
//      cin>>a[i].num;
//      a[i].f=1;
//    }
//    int l=1,r=n;
//    while(k--&&r>=1&&l<=n)
//    {
//       if(a[l].num<a[r].num)
//       {
//         a[l].f=0;
//         l++;
//       }
//       else
//       {
//         a[r].f=0;
//         r--;
//       }
//    }
//    int sum=0;
//    for(int i=1 ;i<=n ;i++)
//    {
//     if(a[i].f==1)
//     sum+=a[i].num;
//    }
//    cout<<sum;
vector<int> a(2*n+1,0);
vector<int> sum(2*n+1,0);
sum[0]=0;
   for(int i=1 ;i<=n ;i++)
  {
     cin>>a[i];
     sum[i]=sum[i-1]+a[i];
  }
  int mx=-1;
  for(int i=1 ;i<=n ;i++)
  {
      int temp=sum[i+n-k-1]-sum[i-1];
      mx=max(temp,mx);
  }
  cout<<mx;
   return 0;
}

// 64 位输出请用 printf("%lld");
